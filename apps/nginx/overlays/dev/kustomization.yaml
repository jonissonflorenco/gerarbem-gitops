apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: gerarbem-dev

resources:
  - ../../base
  - middleware.yaml
  - ingressroute.yaml

configMapGenerator:
  - name: nginx-index
    behavior: replace
    literals:
      - index.html=<!DOCTYPE html><html><head><meta charset="UTF-8"><title>gerarbem</title></head><body style="font-family:Arial;text-align:center;margin-top:80px;"><h1>gerarbem</h1><p>Aplicação provisionada via ArgoCD + GitOps.</p><p>Ambiente: dev</p></body></html>

generatorOptions:
  disableNameSuffixHash: true
